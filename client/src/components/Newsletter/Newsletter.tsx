import styles from "./Newsletter.module.css"

type Props = {}

function Newsletter({}: Props) {
  return (
    <section className={`${styles.main} container`}>Newsletter</section>
  )
}

export default Newsletter